{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "FastAPI (uvicorn) — src.api.app",
      "type": "debugpy",
      "request": "launch",
      "module": "uvicorn",
      "args": ["src.api.app:app", "--reload"],
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal",
      "justMyCode": true
    }
  ]
}
